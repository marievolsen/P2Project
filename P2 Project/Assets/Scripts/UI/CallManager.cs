using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class CallManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //intent.setData(Uri.parse("tel:+4570241207"));
        //startActivity(intent);
    }

}
